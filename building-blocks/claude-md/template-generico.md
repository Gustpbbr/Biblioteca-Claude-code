# CLAUDE.md — <NOME DO PROJETO>

> Template genérico de memória de projeto para o Claude Code. Copie para a raiz
> do seu projeto como `CLAUDE.md`, preencha os `<...>` e remova o que não usar.
> Gere uma primeira versão automaticamente com o comando `/init`.

## Visão geral

<Uma a três frases: o que é o projeto, para quem, qual o objetivo.>

## Stack e estrutura

- **Linguagem/runtime:** <ex: TypeScript / Node 20>
- **Framework:** <ex: Next.js 15>
- **Gerenciador de pacotes:** <ex: pnpm>
- **Pastas-chave:** <ex: `src/` app, `tests/` testes, `scripts/` utilitários>

## Comandos essenciais

- Instalar: `<comando>`
- Rodar em dev: `<comando>`
- Testar: `<comando>`
- Lint/format: `<comando>`
- Build: `<comando>`

## Convenções

- <ex: usar imports absolutos a partir de `@/`>
- <ex: componentes em PascalCase, hooks com prefixo `use`>
- <ex: mensagens de commit no imperativo, em português>
- <ex: sempre adicionar testes para novas funções públicas>

## O que NÃO fazer

- <ex: não editar arquivos gerados em `dist/`>
- <ex: não commitar segredos; usar variáveis de ambiente>

## Contexto adicional

<Links para docs internas, decisões de arquitetura, glossário. Você pode
importar outros arquivos com a sintaxe `@caminho/arquivo.md`.>
